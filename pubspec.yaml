name: detectionobject
description: "A new Flutter project."
publish_to: 'none' # This line prevents accidental publishing to pub.dev

version: 1.0.0+1
environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  camera: ^0.10.5+9
  flutter_tflite: ^1.0.1
  image: ^3.0.1
  flutter_tts: ^3.8.5
  sensors_plus: ^4.0.2
  geolocator: ^10.1.0
  flutter_local_notifications: ^16.3.2





dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  
  uses-material-design: true
  assets:
    - assets/

